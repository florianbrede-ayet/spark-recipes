# ---------------------------------------------------------------------------
# Immutable, content-addressed artifact pins for this recipe.
#
# Every value here was read from read-only audit evidence of the live cluster
# on 2026-08-23 / 2026-08-26. Nothing in this file is a secret.
# Scripts refuse to start a deployment whose live artifacts do not match.
#
# Do NOT "update" these to make a check pass. A mismatch means the deployment
# is not this recipe.
# ---------------------------------------------------------------------------

# --- Model ---------------------------------------------------------------
# Gated Hugging Face repo. The revision is the exact commit that was resolved
# in the live HF hub cache (models--deepseek-ai--DeepSeek-V4-Flash-0731/refs/main).
PIN_MODEL_REPO="deepseek-ai/DeepSeek-V4-Flash-0731"
PIN_MODEL_REVISION="7872f01b1d1fe23eabc4c98b48bffcef5a386062"

# --- Container image -----------------------------------------------------
# Registry reference by digest (immutable) and the resulting local image ID.
# The image ID is what `docker image inspect --format '{{.Id}}'` returns and is
# what the cluster consistency check compares across both nodes.
PIN_IMAGE_REPO="eugr/spark-vllm-b12x"
PIN_IMAGE_DIGEST="sha256:eb3ed2bbb0c91dc6d41282d22532267b5a449088c78a032400cd887fe9ddd2c5"
PIN_IMAGE_REF="eugr/spark-vllm-b12x@sha256:eb3ed2bbb0c91dc6d41282d22532267b5a449088c78a032400cd887fe9ddd2c5"
PIN_IMAGE_ID="sha256:d43f15877df4176dfc70b7ebca336d5de698e1696da02fc3738b0338a65f5db2"
# Local tag the live containers were created from (ConfigImage).
PIN_IMAGE_LOCAL_TAG="vllm-node-b12x"
# Observed image metadata (informational, verified by 60-verify-deployment.sh).
PIN_IMAGE_ARCH="arm64"
PIN_IMAGE_OS="linux"
PIN_IMAGE_SIZE_BYTES="23362056860"
PIN_IMAGE_CREATED="2026-08-05T03:15:53.425328648-07:00"

# --- Vendor framework artifacts (hashes only; files are not vendored) -----
# SHA-256 of the exact live vendor launcher and the exact live recipe YAML.
# The recipe YAML IS vendored, byte-exact, at recipe/deepseek-v4-flash-0731.yaml
# and must hash to PIN_RECIPE_SHA256. The launcher is not vendored — see
# docs/02-prerequisites-and-limitations.md.
PIN_LAUNCHER_SHA256="d587df37af3a37f50328e0e72030177a7e864fff41e4b2edea354c5c6e52dd1a"
PIN_RECIPE_SHA256="2dcd8e9a9448ab51ea8ed6cebfb12961abdb1d278912ca4f0da4e4e8ad98379b"
PIN_RECIPE_FILE="recipe/deepseek-v4-flash-0731.yaml"

# --- Serving topology ----------------------------------------------------
PIN_NNODES="2"
PIN_TENSOR_PARALLEL="2"
PIN_MASTER_PORT="29501"
PIN_API_PORT="8888"
PIN_API_BIND="0.0.0.0"
PIN_CONTAINER_NAME="vllm_node"
PIN_CONTAINER_WORKSPACE="/workspace"
PIN_CONTAINER_EXEC_SCRIPT="/workspace/exec-script.sh"
PIN_CONTAINER_WORKDIR="/workspace/vllm"
PIN_NOFILE_LIMIT="1048576"

# --- Resolved capacity observed at the audited boot ----------------------
# These are OUTPUTS of a specific boot, not inputs. They will differ slightly
# on other boots. See docs/04-capacity-and-health.md for the full caveats.
PIN_API_MAX_MODEL_LEN="1048576"
PIN_LIMITING_KV_TOKENS="2356056"
